dist/
distro/
*.zip

# Generator intermediate. merchant-table.js is the committed artifact; the
# candidate is regenerated on every run and must never be what ships.
scripts/merchant-table.candidate.json
scripts/merchant-table.candidate.json.raw.txt
.probe/

# Chrome writes compiled declarativeNetRequest rulesets here when the
# extension is loaded unpacked. Local build artifact, never source.
_metadata/
